if (document.getElementById("table")) {
	document.getElementById("table").remove();
};

document.getElementById("sidebar").insertAdjacentHTML("afterend", "<%= j render 'mission', missions: @missions, pend_mission: @pending_missions%>");
